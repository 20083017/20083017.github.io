cpu飙升100%
1、perf，查看热点函数

内存飙升100%

2、依据log查看文件中某行log出现的次数，
依据log次数，定位函数位置。

排查代码逻辑，发现是redis存在1个大key。20M。

3、

# top
进程

# top -Hp pid

线程cpu占用

# strace -p pid
内核函数调用情况

# pstack pid

# pid 打开的fd数量
 cat /proc/267/limits 
